[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn InventoryMS.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/login/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
